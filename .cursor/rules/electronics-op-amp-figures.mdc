---
description: 电子学 CircuiTikZ 运放电路图供图、版图与验收（第二章及同系列）
globs: 09-数学-物理-电子/电子/电子学/image/**/*.tex,09-数学-物理-电子/电子/电子学/**/*.md,09-数学-物理-电子/电子/电子学/scripts/build-tex-figure.ps1
alwaysApply: false
---

# 运放电路图（CircuiTikZ）

规范与重建命令：`09-数学-物理-电子/电子/电子学/image/电子学一/figures/02-op-figure-sources.md`  
共享几何：`image/电子学一/source/02-op-circuit-style.tex`

## 工具与流程

1. **只用 LaTeX / CircuiTikZ** 出原理图（`standalone` + `02-op-circuit-style.tex`），与用户笔记里其它运放图一致；勿默认用 Python/schemdraw，除非用户明确要求。
2. 改 `.tex` 后执行：`电子学/scripts/build-tex-figure.ps1 -TexFile "…/source/XX.tex"` → 输出 `figures/XX.svg`。
3. **验收必须看渲染后的 SVG/图**（不是只看代码）：查重叠、斜线、误接地、$R_2$ 与 $R_4$ 共道、标号压线。不满意则调坐标宏后重编译再看。

## 用户供图：应包含什么

| 类别 | 说明 |
|------|------|
| 拓扑 | 每根线谁连谁；结点命名（如 $O$、反馈节点）；**不要**把 $v_+$ 接地画成反相放大器 |
| 运放 | `+` 在上、`-` 在下；中心或锚点坐标（常用 $(0,0)$） |
| 反馈 | 走三角**下方**或**最底水平母线**（$y$ 低于 $R_2$ 接地），再右行 $R_4$、上折接 Output |
| 坐标（推荐） | 关键结点 $x,y$（如反馈节点 $x=-1.5$，底母线 $y=-2.5$，$v_o$ 在 $x=2.5$） |
| 标注 | $R_1,R_2,v_i,v_o$；电阻标在上方 `l^=` / 下方 `l_=` |
| 约束 | 全直角、禁止交叉重叠；可与「和 `02-op-inverting-amplifier` 同风格」 |

**可复制模板（给用户/自己写需求时用）：**

```text
【图名】02-op-xxx-diagram-01
【运放】中心 (0,0)；+ 在上；- 在下
【+ 网络】…（反相放大：+ 仅接地，无电阻）
【- 网络】…（分支结点坐标；← R1 ← vi）
【R2 / R4】…（R2 竖通道 x；R4：结点↓ → y=底 → 右 → x=输出侧 ↑ → out）
【输出】out → vo 端子坐标
【禁止】斜线、穿过电阻、R2 与 R4 重叠、+ 误接地
【验收】与附件/板书一致；编译后看图
```

## 版图硬规则（从反复修图归纳）

- **american**；`noinv input up`（+ 在上）。**勿**对 `noinv input up` 再加 `yscale=-1`（会把 + 翻到底下）。
- 连线仅 `--`、`-|`、`|`，禁止斜穿。
- **分列防交叉**：$R_2$ 竖线在结点 $O$ 的 $x$；反馈分支点在**更左**的 $x$（如 $O$ 在 $-0.65$、反馈在 $-1.5$），$R_4$ 走 $y$ 最低的通道。
- 重要结点用 `\node[circ, fill=black, inner sep=0.9pt]`；命名结点标 `opfigThin`。
- 反相放大：$v_+\to$ 地；$v_i\to R_1\to v_-$；$R_2$ 为 $v_o\to v_-$ 反馈，在底母线绕行。
- 四电阻例题：$v_1\to R_1\to O\to v_+$，$O\to R_2\to$ 地；$v_2\to R_3\to v_-$；$R_4$ 负反馈到 $v_-$。

## 实现要点

- 复杂图用**绝对坐标** `\coordinate (VM) at (-1.5, -0.5);`，引脚用 `let \p1=(OA.+)` 对齐 $y$ 后水平接入。
- 图专用间距写在**该 .tex 顶部宏**（如 `\OpExXfb`、`\OpExYbus`），勿为单图改全局 `02-op-circuit-style.tex`。
- TikZ：`anchor={-}` 勿写 `anchor=-`；`OA.-` 与 `(OA.-|- FBLOW)` 等 calc 语法要合法。
- 不提交 `.aux`/`.pdf` 构建产物。

## 与笔记联动

- 图路径：`image/电子学一/figures/02-op-….svg`；笔记 `电子学一第二章.md` 用相对路径引用。
- 正文写结论；长推导在附录（见 `read-course-notes` 规则）。
