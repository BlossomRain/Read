# handmade 代表图 — 版式规范摘要（Agent 用）

**交稿目录：** `电子学/handmade/`  
**预览：** `01`–`05` 的 `.tex` / `.svg`；原始 Designer 导出：`*.tikz`  
**状态：** 2026-06-05 用户确认「整体符合预期」，可据此启动第二章运放图批量重绘。

**Git 检查点（改图前回退用）：** `5147762` — `Checkpoint before batch op-amp figure sync from handmade layout.`  
回退：`git checkout 5147762 -- image/电子学一/source/02-op-*.tex` 或 `git reset --hard 5147762`（慎用）。

---

## 用户版式（与 `designer-sample-brief` 正式规范的差异）

| 项目 | 用户 handmade | 仓库正式 `02-op-*.tex` |
|------|---------------|------------------------|
| 运放朝向 | Designer 默认 `\node[op amp]`：**上 −、下 +** | `noinv input up` 或 `down`+`yscale=-1`：**上 +、下 −** |
| 反馈走线 | **顶线母线**（约 $y=7.5$ / $7.25$） | 反相类多走 **底母线** |
| 接线方式 | **固定坐标** `(6.56, 6.24)` 等，少用 `OA.+` 锚点 | 锚点 + `02-op-circuit-style.tex` 宏 |
| 标注 | 独立 `\node` 放 $R,C,v_i,i_i$；**勿** `l_=$+$`、`i=$i_i$` 贴在 bipole 上（易与运放 ± 叠字） |

### 批量重绘策略（已定稿）

**镜像 handmade → 正式 CircuiTikZ**（用户 2026-06-05 确认）：

| 从 handmade | 镜像到正式图 |
|-------------|--------------|
| 上 −、下 + | **`noinv input up`：上 +、下 −**（上下交换 ±） |
| 顶线反馈（反相/同相/积分） | **底母线反馈**（与 `02-op-inverting-amplifier` 等一致，线不易交叉重叠） |
| 跟随器顶线回 − | **反馈走三角下方**（`OpFbClear` / 底通道，见 `02-op-voltage-follower-diagram-01`） |
| 拓扑与结点关系 | **不变**（谁连谁、$R_1$/$R_2$ 分工、双面板 `||`） |

handmade 仍作**拓扑参考**；正式 `.tex` 不复制 Designer 顶线坐标，而用 `02-op-circuit-style.tex` 宏 + 锚点实现镜像后版式。

---

## 五张代表图映射

| handmade | brief ID | handmade 拓扑 | 正式图实现 |
|----------|----------|---------------|------------|
| `01-follower` | `sample-ch2-01` | $v_i$→+；$v_o$ 负反馈 | + 在上；$v_i$→+；反馈**下方**→− |
| `02-inverting` | `sample-ch2-02` | $v_i$→$R_1$→−；+ 地；$R_2$ 回 $v_o$ | + 在上仅接地；$R_2$ **底母线** |
| `03-non-inverting` | `sample-ch2-03` | $v_i$→+；− 上 $R_1$/$R_2$ | 同拓扑；$R_2$ 可走输出侧竖线（现有 `02-op-non-inverting`） |
| `04-integrator` | `sample-ch2-05` | 反相 + 反馈 $C$ | 同 `02-op-integrator-diagram-01`（底母线 $C$） |
| `05-iv-model` | `sample-ch2-04` | I–V + model 双面板 | 对齐 `02-op-iv-converter-model`；补 $R_L$ |

---

## 是否足够批量重绘？

### 结论：**第二章主体足够启动；缺 1 项建议、2 项可选**

| 评估 | 说明 |
|------|------|
| **足够** | 5 张必做类型齐全；顶线反馈、双面板、电流源输入、model 分立顶线等意图明确 |
| **建议补** | `sample-ch2-06-vi-left`（V–I 左半）：无图也能从 `05`+笔记推，但有 1 张更稳 |
| **可选补** | model 侧 **$R_L$**（`05` 右半未画）；**JSON** 便于 Designer 再改 |
| **不必再画** | 微分器、积分+$R_f$、h 参数、仪表放大器、街灯链、EKG 框图、Bode/波形 |
| **第一章** | 未交代表图；运放规范定稿后再做 Ch1 四张 |

### 右 model 与 brief 的差异（重绘时按 brief 补全）

- brief 要求 **$R_i$ 顶与 $A i_i$ 顶断开** — `05` 右半已分立，可沿用。
- brief 要求 **$R_L$** — 当前 `05` 未画，正式 `02-op-iv-converter-model-diagram-01.tex` 有，重绘时补上即可。

---

## 标注与编译注意

1. 编译：`handmade/build-preview.ps1`（输出同目录 `.svg`）。
2. 勿对靠近运放的 bipole 使用 `l_=$+$`、`i=$…$`。
3. 反相/积分：$R_2$ / $C$ 走 **底母线**（`FBL`→`FBR`→输出），勿再抄 Designer 顶线坐标。
4. I–V 左半：$i_i$ 用端子 + `to[I]`，标签放左侧 `\node`。

---

## 下一步

1. ~~版式策略~~ → **镜像 ± + 底母线/下方反馈**（已定）。
2. 按本摘要核对并统一 `image/电子学一/source/02-op-*.tex`，`build-tex-figure.ps1` 重编译 SVG。
3. 验收：渲染后查重叠、斜线、误接地；标注用独立 `\node`，不用 `l_=$+$` / `i=$…$` 贴 bipole。
